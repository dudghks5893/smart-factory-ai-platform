apiVersion: v1
kind: ConfigMap
metadata:
  name: smartfactory-api-config
  labels:
    app.kubernetes.io/name: smartfactory-api
    app.kubernetes.io/part-of: smartfactory-ai-platform
    app.kubernetes.io/component: configuration
    app.kubernetes.io/version: "0.1.0"
data:
  MODEL_DEVICE: cpu
  MAX_UPLOAD_BYTES: "10485760"
  PATCHCORE_ARTIFACT_DIR: /runtime/model
  PATCHCORE_THRESHOLDS_PATH: /runtime/thresholds/thresholds.json
