apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartfactory-api
spec:
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 6Gi
              nvidia.com/gpu: 1
